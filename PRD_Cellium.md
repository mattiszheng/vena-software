# PRD — Cellium：用 2D 空間取代資料夾的視覺化檔案整理層

> 狀態：草稿 v0.5
> 日期：2026-05-15
> 作者：待填
>
> **v0.5 主要變更（質的轉向）**：
> 1. **核心定位重寫**：從「graph-first + 多模態知識編輯器」改成「**better Finder / Explorer：用 2D 空間取代資料夾的視覺化整理層**」。對標關係是「Notion 之於 Word」——不取代 Finder，是第三種使用檔案的方式。
> 2. **目標場景具體化**：個人桌面亂、手機檔案散落、雲端 drive 整理。痛點普遍存在，不再侷限於「視覺思考者」這個小眾。
> 3. **架構決策：寄生在 drive 上做同步**：cellium.json 放進使用者既有的 Drive 同步資料夾，drive 客戶端負責檔案+json 同步。Cellium 不做後端，同步問題外包。
> 4. **AI 角色重新定位**：從「翻譯層」（PRD §4.8 v0.4）改成「**Spotlight 式輔助**」——AI 不替使用者分類，是「找不到時的後備」。
> 5. **空間 vs 連線的優先級重排**：核心動作是「拖檔案進來、自由排」，空間位置即關係。連線退到進階功能。
> 6. **商業模式根本原則：「錢只能在對的地方收」**——付費是便利或服務的對價，不是進入權。具體五個付費點：commercial license、app store 買斷、AI 雲端推論訂閱、多人即時協作訂閱、Cellium 官方 Sync 訂閱（drive 同步替代品）。
> 7. **三條 Provider 並存**：LocalStorageProvider（含 drive 同步特例）、CloudStorageProvider（官方 Sync）、SelfHostedProvider。
> 8. **必須驗證的硬性條件**：MVP 完成後找非技術朋友試用，驗證「桌面亂」痛點普遍存在，不能只靠 dogfooding。
> 9. **附錄新增**：v0.5 方向轉折脈絡（從「graph-first 知識編輯器」到「better Finder」的推導過程）。

## 一、背景與問題

知識工作者在多個領域都面臨同一個結構性問題：**設計決定、研究素材、論證脈絡散落在多個工具裡，沒有一個地方能同時做到豐富的內容格式 + 文件之間的關係可視化**。

具體場景：

- **遊戲設計（GDD）**：設計散落 Obsidian / Notion / txt，出現 v1、v2、v1重製、v1重製final
- **軟體設計（SDD）**：架構圖、系統關係、模組依賴跨多個工具
- **文獻管理 / 論文編輯**：三種文獻群之間有直接引用、間接引用，又要邊讀邊做筆記、邊建關係圖
- **個人知識管理**：跨主題的概念連結、反駁、延伸關係難以表達

**核心痛點是同一個：缺乏一個 graph-first 的知識編輯器**，現有工具都是 document-first 加上附屬的關係功能。

現有工具的缺口：

- **Obsidian**：關係圖好，但格式受限只能 Markdown，關係只是無向連結
- **Notion**：格式豐富，但關係太淺，只有階層沒有橫向語意
- **Heptabase / Scrintal**：白板 + 卡片接近，但節點類型固定不夠泛型
- **Zotero / Connected Papers**：文獻場景強，但視覺化不可編輯
- **tldraw / Miro**：自由度高，但無結構化資料模型

---

## 二、目標用戶

**主要用戶：跨領域的個人知識工作者**

定位寬廣，但有共同特徵：

- 處理的資訊有複雜橫向關係（不只是階層）
- 願意花時間建構自己的知識結構
- 對資料主權敏感，傾向本地優先

具體族群：

- **獨立遊戲開發者**：一人扮演設計師、規格定義者、邏輯定義者
- **學術研究者**：管文獻網絡、論證結構、跨論文關聯
- **小型遊戲工作室 PM / Lead Designer**（1–10 人）：跨崗位文件關係
- **重度知識工作者**：用 Notion 但覺得「不夠用」、需要表達 Notion 階層處理不了的關係
- **開發者 / 技術型使用者**：要可以 grep、可以版控、可以寫腳本處理自己資料

**不是目標用戶：**

- 大型企業團隊（Notion 已經服務得很好，無法競爭）
- 純文件編輯需求（不需要關係，用 Notion / Word 即可）
- 不願意學習新工具的人（轉換成本太高）

---

## 三、產品定位（v0.5 重寫）

> **Better Finder / Explorer：用 2D 空間取代資料夾的視覺化檔案整理層**。本地優先、開源、不鎖定使用者。任何檔案類型都能用空間記住它們的關係。

### 3.1 對標關係：Notion 之於 Word

Cellium 之於 Finder 的關係，就像 Notion 之於 Word/Notepad：

- Notepad / Word 沒有死，Notion 也活得很好
- 使用者照樣用 Word 寫正式文件，但多了一個 Notion 處理「自己長期累積的知識/專案」
- Notion 不取代前者，定義了「第三種使用文字的方式」

Cellium 同理：

- Finder / Explorer 不會死，作業系統內建工具不能也不該被取代
- 使用者照樣用 Finder 處理日常檔案瀏覽，但需要「整理一組相關檔案在一起」的時候，目前沒有好工具
- Cellium 定義「第三種使用檔案的方式」：用 2D 空間記憶取代資料夾巢狀結構

### 3.2 真正解決的問題

**「我想把一組相關的東西放在一起，之後能一眼看到全局」**

具體場景：

- 一個專案的素材（文件、參考圖、PDF、截圖）
- 一次旅行的東西（行程 PDF、訂房截圖、票券、地圖圖片）
- 一個研究主題的資料（論文、筆記、數據、引用）
- 一個正在思考的問題（草稿、相關文章、靈感截圖）

這些場景的共通特徵：**多種檔案類型 + 有意義的關係 + 需要保留全局視野**。Finder 不夠（強制排序、平面列表），資料夾不夠（巢狀藏起來），桌面不夠（沒結構、會被新檔案蓋掉），Stacks 不夠（系統規則分組，不是使用者意義分組），雲端 Drive 也不夠（同步做得好但組織完全沒解）。

### 3.3 核心使用者價值：空間記憶取代規則排序

**Stacks 把你的桌面藏起來。Cellium 讓你用空間記住東西。**

實體世界你不會忘記「我把鑰匙放在玄關桌上」，因為空間位置本身就是記憶錨點。Stacks 把這個能力剝奪了（系統幫你決定怎麼分），資料夾把這個能力剝奪了（深層巢狀，看不到全局），Finder 平面檢視也把它剝奪了（強制按名稱/日期排序）。

Cellium 還給使用者空間記憶：
- 最重要的放中間
- 相關的放一起
- 暫時的放角落
- 一個檔案可以同時出現在多個畫布

### 3.4 不是什麼

- **不是 Notion 的競品**：Notion 處理結構化文件和 wiki，Cellium 處理檔案本身的關係
- **不是 Obsidian 的競品**：Obsidian 是 Markdown vault + wikilink，Cellium 接的是任何檔案類型，不限 .md
- **不是 Heptabase / Kosmik 的競品**：他們是「visual canvas 工具」（給空白畫布讓使用者自己決定怎麼用），Cellium 是「檔案整理工具」（打開就有使用者既有的檔案在裡面）
- **不是 Finder 的取代品**：Finder 是 OS 級別整合，永遠不該被取代。Cellium 是疊加層

### 3.5 v0.5 重要校正：對標型產品思維是錯的

Heptabase 在 2023 年寫過 Heptabase vs Notion vs Obsidian 的文章，把自己放在跟 Notion/Obsidian 同框比較——這代表他們的自我定位是「visual canvas 版的 Notion」。Kosmik 對標 Figma 也是同樣邏輯。

對標型思維的本質是**從別人的座標系裡找自己的位置**。Notion 不對標誰，它定義了「all-in-one workspace」這個新類別。Obsidian 也不對標誰。這兩個贏的產品都沒有在跟誰比，是別人在跟它們比。

Cellium v0.5 之前的論述（「graph-first 多模態知識編輯器」）也犯了同樣的錯——它在跟 Obsidian 比資料模型、跟 Kosmik 比視覺化、跟 Notion 比節點豐富度。v0.5 的論述（「better Finder：用 2D 空間取代資料夾」）不對標任何競品，直接從一個普遍痛點出發：你的桌面亂、你的雲端硬碟找不到東西。

### 3.6 兩個用戶群入口（不互斥）

從不同入口進來的使用者看到的論述不同，但都導向同一個產品：

| 入口 | 論述 | 主要場景 |
|---|---|---|
| 個人桌面亂 | 「你的桌面亂嗎？用 Cellium 重新組織。」 | 本地檔案、混合類型、自由排佈 |
| 雲端 drive 整治 | 「你的 Google Drive 找不到東西嗎？Cellium 給你視覺化整理層。」 | drive 同步資料夾、團隊共享 |

多數使用者兩種痛點都有，產品同一個，只是 marketing 入口分流。

### 3.7 競品對比（v0.5 重畫）

| | Finder/Explorer | Stacks (macOS) | 第三方檔案管理員<br>(Path Finder, ForkLift) | Heptabase/Kosmik | Cellium |
|---|---|---|---|---|---|
| 核心範式 | 樹狀資料夾 | 自動分類堆疊 | 加強版樹狀 | 空白 visual canvas | 2D 空間整理層 |
| 進入點 | OS 內建 | 桌面 | 替代 Finder | 空白畫布 | 使用者既有檔案 |
| 解決問題 | 檔案瀏覽 | 桌面視覺整理 | 進階檔案操作 | 視覺化思考工具 | 把相關檔案放一起 |
| 對使用者要求 | 記得路徑 | 信任系統分類 | 學新工具 | 自己想要做什麼 | 拖檔案到一起 |
| 多模態支援 | 有 icon、無語意 | 按類型自動分 | 同 Finder | 內嵌預覽（重工程） | icon + 雙擊開檔 |
| 同步 | OS 機制 | OS 機制 | OS 機制 | 自家後端 | 寄生在 Drive 上 |

## 四、核心產品決策（v0.2 新增）

這次討論定下的關鍵決策，按優先級排序：

### 4.1 資料模型：三種物件平權

論文（檔案）、筆記、群組——**三者都是平權的 2D 物件**，不是誰附屬於誰。

- 一條筆記可以連到多篇論文
- 一個論文可以同時屬於多個群（群是 tag-like，不是互斥資料夾）
- 筆記可以連到筆記（例如「筆記 A 反駁筆記 B」）
- 任何兩個節點都能連線

### 4.2 邊（關係）的設計

- 任何兩個節點都能連線，**無類型限制**
- 邊的類型由**使用者用顏色、樣式（實線/虛線/粗細）自賦語義**
- 系統不預設「直接引用 / 間接引用 / 反駁」這種固定類型——這是文獻場景特有，做成預設就把產品鎖死在文獻管理了

### 4.3 層級：節點即子畫布

不用側邊欄樹狀結構處理層級，而是**節點本身可以是容器**：

- 收起時：是一個節點，顯示名稱、可能有縮圖或摘要
- 展開時：進入這個節點的內部，看到裡面的子節點和它們的關係
- 概念類比：iOS 相冊（平常是封面，點開瀏覽內部）、Finder 資料夾

**展開方式由使用者選：**

- 原地展開（在當前畫布展開）
- 新開視窗（像 Finder 雙擊資料夾、或拖到旁邊開新視窗）

### 4.4 筆記粒度

**論文層級**，不做行間註解（不是 Zotero better notes 那種）。

每篇文獻可建立一個自己的筆記，再用關係圖連結。行間註解延後，不是 MVP 範圍。

### 4.5 圖表：可編輯（v0.5 重定位為進階表達力）

使用者可以直接在 2D 畫布上：

- 拖拉節點（**核心動作**：空間位置即關係）
- 自己連線（**進階動作**：表達跨距離關係）
- 手動標記邊的類型（顏色/樣式）
- 展開/收起層級

**v0.5 校正**：v0.4 把連線列為「核心差異化」，但重新思考之後，使用者的核心動作是「拖檔案進來、自由排」，不是「建節點和線」。空間位置本身就承載大量意義（重要性、相關性、時序），不需要連線就有價值。

連線的真正角色是**空間表達力的擴充**——當使用者想表達「這兩個東西即使放得遠也是有關的」時用。沒有連線，使用者只能靠位置表達關係，會受畫布尺寸限制。

對核心使用者價值的影響：**第一次打開 Cellium，使用者該看到的是「拖檔案進來」，不是「建立節點」。** Onboarding 設計依此調整。

### 4.6 檔案格式：套渲染層在原檔案上

不發明新格式，而是**在使用者既有檔案上套一層渲染**：

- **論文 PDF**：留在使用者電腦原位置，app 不動它
- **筆記**：用 Markdown（.md）檔，使用者用任何編輯器都能打開
- **關係圖、節點位置、顏色標記、層級結構**：一個 JSON 檔（或 SQLite）記錄

**斷鏈處理（業內標準做法組合）：**

1. 工作區根目錄概念 + 相對路徑（像 Obsidian vault）
2. 檔案 hash 當備援（路徑失效時掃描重連）
3. 檔案監聽（Finder 改名/移動時自動更新）
4. 真斷鏈時優雅降級（顯示「⚠️ 找不到檔案」讓使用者手動指認）

### 4.7 多模態節點：檔案總管式（v0.4 確定）

非 .md 的檔案類型（PDF、CSV、PPT、PNG、影片、音樂等）在畫布上的呈現策略：

**MVP 階段不做內嵌預覽**，採檔案總管式：

- 顯示對應 extension 的 icon + 檔名 + 修改時間
- 雙擊用系統預設應用打開（Electron `shell.openPath()`）
- 在右欄 Inspector 顯示客觀 metadata（檔名、大小、類型、修改時間，以及 4.8 提到的 AI 翻譯結果）

**唯一例外：圖片做縮圖**。因為圖片的視覺判斷不靠檔名靠內容，沒縮圖就失去意義。

**為什麼採這個策略：**

- Kosmik / Heptabase 級的內嵌預覽（PDF 內頁、PPT 投影片）每種檔案類型都是獨立的月級工程，個人開發者吃不下
- 「icon + 雙擊開檔」是 OS 已經解決過的問題，使用者已經有肌肉記憶
- 渲染層概念在「多模態」這個範圍內依然成立——關鍵是讓檔案能變成圖上節點，不是讓畫布變成檔案瀏覽器

之後想做特定檔案類型的深度整合（例如 PDF 內頁節點、CSV 表格節點）可以做為 plugin 開放給社群實作（PRD §6.3）。

### 4.8 AI 角色：Spotlight 式輔助（v0.5 重新定位）

**v0.5 重要校正**：v0.4 把 AI 設計成「翻譯層」——讀檔案產出客觀 metadata（作者、時間、實體、主題）讓使用者校對。但這個設計有兩個問題：

1. **AI 自動分類跟 Stacks 自動分組是同一種東西**，差別只在 AI 比較聰明。使用者三個月後回來看到 AI 分的類，還是不認得，因為那不是他自己的分類。
2. **使用者真正的需求不是「自動整理」，是「找得到」**。Spotlight 不替使用者整理，只是「你想找的時候找得到」。這個心智模型對使用者熟悉太多。

v0.5 的 AI 定位：

- **不替使用者分類**：分類權保留給使用者，AI 不主動動畫布結構
- **不替使用者建立關係**：知識工作的核心是「建立詮釋」，這個外包就違背產品價值
- **當「找不到時的後備」**：使用者問一句「上週做簡報用的那個圖表在哪」，AI 翻畫布找到
- **當「想不起來時的提醒」**：「這個畫布你三個月沒看了，要不要清理」「這個檔案在 5 個畫布裡都有出現，可能是核心素材」

**對齊 Claudian 的精神**：使用者保留「要不要信 AI」的決策權。AI 是可選的、輔助的、查詢用的，不是核心功能。

**AI 該做和不該做：**

| AI 該做 | AI 不該做 |
|---|---|
| 自然語言搜尋（找東西） | 自動分類（替使用者組織） |
| 為檔案生成可選的描述標籤 | 自動建立節點之間的關係 |
| 提醒久未動的畫布 | 強制套用某種結構 |
| 跨畫布找重複出現的素材 | 替使用者寫詮釋 |

**待決定**：

- AI 推論成本承擔方式（使用者自帶 API key、本地小模型、官方雲端訂閱、混合？）
- 觸發時機（使用者主動查詢、檔案 import 時做基礎索引、定時 refresh？）
- 索引資料的存放（cellium.json 還是另外存？）

---

## 五、商業模式（v0.5 重新定義）

### 5.1 根本原則：錢只能在對的地方收

> **付費是「具體服務或便利」的對價，不是「使用權」的門票。**

這個原則決定了一切。Notion 和 Obsidian 個人版同步都免費，因為個人同步成本對他們微不足道，而個人使用者帶來的口碑和潛在 B2B 轉化遠超那點伺服器費。Heptabase / Kosmik 收的是入場費，使用者一進來就要付——這個錢是「軟體的存在」費用，沒有對應到任何具體服務。結果是使用者轉身離開。

**測試方法**：每一個付費點，問「使用者付的是什麼具體東西」。答得出來就收，答不出來就不收。

具體應用到 Cellium：

**不對的地方（不收）**：
- 軟體本體（單機使用、本地檔案、自己整理畫布）
- 個人使用的同步（用 Drive 自己同步）
- 基礎功能（畫布、節點、連線、Markdown 編輯）
- 使用上限（不做「免費版只能 100 個節點」這種飢餓行銷）

**對的地方（收）**：每個都對應到具體的服務或便利，使用者付得心甘情願。

### 5.2 五個付費點

| 付費點 | 對應價值 | 為什麼是「對的地方」 |
|---|---|---|
| **Commercial license（B2B）** | 公司合規使用授權 | 法律風險的對價，跟 Obsidian 一樣靠誠信制 |
| **App store 一次性買斷** | 官方打包版 + 自動更新 + 公證 | 「不想自己 build」的便利費 |
| **AI 雲端推論訂閱** | 不用自帶 API key 的便利 | 自帶 key 永遠是免費選項 |
| **多人即時協作訂閱** | 即時 server 中繼（看到對方游標、即時編輯） | drive 同步做不到的，真實服務 |
| **Cellium 官方 Sync 訂閱** | E2E 加密 + 一鍵設定 + drive 替代品 | 不想用 drive 的人的便利，自架永遠免費 |

**五個付費點的共通特徵**：使用者可以選擇不付。技術型使用者完全可以自帶 API key、自己編譯、自架伺服器、個人使用不需要 commercial license。付費是「便利的對價」，不是「使用的門票」。

### 5.3 三層使用者，三條路徑

| 層 | 使用者 | 路徑 | 付費 |
|---|---|---|---|
| L1 純本地 | 個人、單裝置 | 下載免費版，本地用 | 全免費 |
| L2 多裝置 | 個人、跨裝置 | 用 Drive 同步 vault | 全免費（drive 是別人的事） |
| L2' 多裝置加強 | 不想用 drive 的人 | Cellium 官方 Sync | 訂閱 |
| L3 團隊 | 1-10 人小團隊 | 共享 drive 資料夾 + 多人協作訂閱 | 訂閱（協作部分） |
| L4 企業 | 公司部署 | Commercial license | per-seat 年費 |

L1 和 L2 是 90% 的使用者，都免費。L2'、L3、L4 是 10% 的使用者，是收入來源。

### 5.4 寄生在 drive 上的同步策略（v0.5 新增）

**Cellium 預設不做後端同步，寄生在使用者既有的 drive 上。**

工作流：
- 使用者把 vault 放進 Google Drive / Dropbox / iCloud / OneDrive 的同步資料夾
- drive 客戶端負責同步檔案 + cellium.json
- Cellium 本地讀檔案、讀 cellium.json，完全不碰同步
- 跨裝置：drive 同步好之後，Cellium 在另一台機器讀本地（已同步）的檔案

這個架構的優雅之處：**把同步問題完全外包給已經解決這個問題的人**。Cellium 只負責它擅長的事（視覺化、空間排佈），同步這種苦工讓 drive 做。

cellium.json 衝突處理（drive 偶爾會產生 conflicted copy）：
- 把 cellium.json 拆成多個小檔案（每個畫布一個），減少衝突機率
- 用 CRDT-friendly 的資料結構讓自動合併容易
- 真衝突時顯示「畫布有衝突，請選擇版本」UI

### 5.5 為什麼這個立場成立

- **商業誠實感**：使用者覺得被公平對待，黏著度高
- **篩選對的客戶**：技術型使用者免費用 + 寫 plugin + 傳教，付費使用者主動選擇換方便
- **競爭對手學不來**：Notion 不可能說「個人同步靠 drive 就好」，整個架構不容許
- **公平感是真正的護城河**：使用者付得心甘情願

### 5.6 必須誠實面對的代價

- 營收上限較低，主要客戶是 B2C 個人加 B2B 小團隊，做不到 Notion 那種百億美元規模
- 對齊參考：Obsidian 18 人 bootstrapped 做到 $25M ARR，這是 Cellium 可以期待的天花板量級
- 邊界要事先想清楚：AI 推論算服務嗎（算，免費版要使用者自帶 key）？協作能不能在自架環境跑（可以，但官方版方便）？

### 5.7 參考標竿

最接近的模式是 **Obsidian**（commercial license + Sync 訂閱 + Publish 訂閱 + Catalyst 一次性買斷）。Obsidian 本身沒開源，Cellium 比它更開放。

Logseq / AppFlowy（完整開源 + 雲端服務收費）也是參考。

### 5.8 長期方向：規則制定者位置（v0.4 沿用）

**這是十年級的戰略方向，不是 MVP 範圍。但會影響中期的格式設計。**

Zotero 真正贏的位置不是「reference manager」，是 **CSL（Citation Style Language）** —— 整個學術引用的「書同文車同軌」。

**多模態知識管理有類似的規則真空**：

- 一個 PDF 在 Zotero 的 highlight、Apple Books 的 note、Adobe 的 annotation 互不相通
- 一張圖在 Photos 的 album、Lightroom 的 keyword、檔案系統的資料夾，三套 metadata 各自為政
- Windows + Mac 同步的痛苦根源就是「檔案 + metadata + 應用狀態」沒有共通格式

如果 Cellium 的 cellium.json schema 能設計成跨檔案類型、跨工具流通的開放格式，就有機會佔到「多模態 metadata 之於 CSL」的位置。

**對 MVP 的影響**：cellium.json schema 設計時保留「未來可被其他工具讀寫」的可能性。具體：

- Schema 用清楚版本號
- 不放任何 Cellium app 專屬的 UI 狀態（那是 settings 的事）
- 文件 + 開源（時機到時）是把採用門檻降到最低的方式

## 六、開源策略（v0.2 新增）

### 6.1 核心立場

**開源 + plugin 支援 + 只收伺服器費**。

跟前面所有決定（檔案格式開放、本地優先、商業模式誠實）邏輯一致——不是另外加的策略，是現有決定的自然延伸。

### 6.2 為什麼開源能解決「轉換成本」這個死穴

工作流工具的轉換成本極高（資料遷移、肌肉記憶、生態鎖定、信任）。開源是**最高等級的信任承諾**：「公司倒了 code 還在、資料還在、社群可以接手」。這種承諾閉源產品給不起。

### 6.3 Plugin 解決「通用工具什麼都做不好」的死穴

通用工具最大的問題是「什麼都能做、什麼都做不好」。開放 plugin 後：

- 文獻場景社群會做 Zotero 整合 plugin
- GDD 場景會做遊戲設計範本 plugin
- 論文場景會做 LaTeX 匯出 plugin

**不用做完所有場景，社群會幫忙做。**

### 6.4 開源時機

**MVP 階段先不要急著開源。**

先自己用、做到夠好、確定產品方向，再開源。太早開源會被社群意見拉著跑、core 還沒穩就要維護外部 PR，反而拖慢進度。

**Obsidian 從來沒開源、Logseq 開源時已經有清楚的產品方向。**開源是策略時機問題，不是越早越好。

### 6.5 開源邊界

需要事先想好的問題（待決定）：

- [ ] CRDT 同步引擎要不要開源？開源可能讓使用者自架繞過付費
- [ ] Obsidian 處理方式：客戶端閉源、Sync 閉源；但 plugin API 完全開放
- [ ] 我們的方向比 Obsidian 更開放，但伺服器服務的核心邏輯可能要保留閉源

---

## 七、MVP 範圍（v0.5 修訂）

**目標：自己重度使用 + 找 3-5 個非技術朋友試用，確認「桌面亂痛點是普遍的」這個假設。**

v0.5 校正：MVP 目標從「自己重度使用 3 個月」改成「**自己用 + 真實使用者測試**」。理由見 §九 動力來源校正——這是商業驅動而非 dogfooding 驅動的項目，必須及早做外部驗證。

### 7.1 v0.1 已完成（Session 01）

- ✅ 2D 畫布（拖拉、縮放、平移，React Flow）
- ✅ Markdown 編輯器（左寫右預覽）
- ✅ WikiLink `[[檔名]]` 自動生成連線
- ✅ 本地檔案讀寫（Electron）
- ✅ 工作區概念 + 相對路徑
- ✅ 工作區記憶（上次路徑自動載入）
- ✅ Light / Dark 主題、中/英 i18n
- ✅ Settings 面板、Inspector 右欄
- ✅ StorageProvider 抽象層 + LocalStorageProvider 實作
- ✅ cellium.json 存關係（不動原始 .md）

### 7.2 v0.5 納入 MVP（剩餘）

**核心整理功能**：
- 多模態節點：非 .md 檔案以 icon + 檔名顯示，雙擊系統預設應用開啟；圖片做縮圖
- 拖檔案到畫布即建節點（從 Finder 直接拖進來）
- 自由排佈、群組、視覺記憶輔助（節點間距、對齊輔助線）

**進階表達力（次要優先級）**：
- 手動連線 + 邊顏色/樣式
- 泛型節點：底層統一資料模型，前端區分顯示為檔案 / 筆記 / 群
- 節點原地展開為子畫布

**Onboarding（v0.5 新增、必做）**：
- 第一次打開的範本選擇：旅行整理範例、專案素材範例、論文研究範例
- 從範例改成自己的，比從空白畫布開始好用十倍
- 空狀態提示：「拖檔案進來開始」
- 不出現 pricing、訂閱、買斷字眼（理念對齊 §5.1）

### 7.3 不在 MVP

- Block editor
- 多人協作（任何協作邏輯）
- 版本追蹤（git-like 功能延後）
- **AI Spotlight**（v0.5 方向感確定，MVP 不做；先做純人工版）
- Cellium 官方 Sync（先寄生 drive，自家 Sync 之後做）
- 行動端
- Plugin 系統（先驗證核心、再開放擴展）
- 開源（先自己用穩、再開）
- 行間註解 / 段落層級節點
- **任何付費機制**（MVP 階段完全免費，理念對齊 §5.1）

### 7.4 MVP 完成判準（v0.5 修訂）

兩個並行的判準，都要過：

**判準一：自己用會不會痛**

「**現在不做這個，我自己用會不會痛？**」會痛就做、不會就放著。

**判準二：非技術朋友第一次打開會不會卡住**

v0.5 新增的硬性條件。MVP 完成後找 3-5 個非技術朋友試用，觀察：

- 第一次打開後 5 分鐘內，他知道要幹嘛嗎？
- 他願不願意把自己手上的檔案拖進來試試看？
- 一週後他會自己回來開 Cellium 嗎？

**這些問題沒辦法靠 dogfooding 回答**，必須真實測試。如果使用者第一次打開卡住，產品方向有問題，繼續做也是浪費。

### 7.5 驗證目標（v0.5 新增）

MVP 完成不是目標，**驗證對賭**才是。具體要驗證：

1. **桌面亂的痛點是普遍的**——非技術朋友是否會說「啊我也有這個問題」
2. **2D 空間記憶比資料夾好用**——使用者三個月後回來能否找回自己放的東西
3. **使用者願意主動投入時間整理**——這是 v0.5 跟「層次三：建立關係」的最大差異，「拖檔案放一起」的門檻比「建關係圖」低很多，但是不是足夠低還要看

驗證失敗的話有兩條路：產品方向再調整、或承認賭錯接受結果。**不能用「使用者還沒理解產品價值」當藉口繼續做下去**。

## 八、技術架構（v0.2 修訂）

### 8.1 Stack

考量是 vibe coding 開發，選最主流、AI 最會寫、最容易 debug 的選項：

- **語言**：TypeScript 全棧
- **桌面**：Electron（不選 Tauri，雖然更好但 AI 寫得沒 Electron 順）
- **前端**：Vite + React
- **畫布**：React Flow 或 tldraw SDK（先用現成的，不自己刻 canvas）
- **MD 預覽**：marked.js
- **CSS**：Tailwind
- **協作（後期）**：Yjs 或 Automerge（CRDT），考慮直接用 Liveblocks / Partykit 把難的部分外包

### 8.2 架構原則：Core / Shell 分離（長期目標）

**長期目標**：核心邏輯獨立成一層（headless core），UI 各平台各做各的。

- **Core 層**：資料模型、CRDT 引擎、檔案 IO、操作 API（純 TypeScript）
- **Shell 層**：Web（React + Canvas）、Desktop（Electron）、Mobile（React Native）

**MVP 階段**：先全部混在一個 repo 裡，等產品方向確定再拆。**過早分層會讓 AI 在多個檔案間迷路。**

### 8.3 StorageProvider 抽象層（v0.5 三條路線並存）

文件讀寫透過統一 interface，UI 層不直接碰文件系統：

```ts
interface StorageProvider {
  read(fileId: string): Promise<string>
  write(fileId: string, content: string): Promise<void>
  list(folderId?: string): Promise<FileNode[]>
}
```

v0.5 並存三條 Provider，使用者第一次設定時選擇：

- **LocalStorageProvider** — 接本地檔案系統
  - 預設選項
  - drive 同步是這個 Provider 的特例：vault 放在使用者既有的 Google Drive / Dropbox / iCloud 同步資料夾裡，drive 客戶端負責跨裝置同步
  - Cellium 完全不碰同步邏輯

- **CloudStorageProvider** — Cellium 官方 Sync 後端（付費）
  - 給不想用 drive 的使用者
  - E2E 加密、一鍵設定
  - 商業模式對應 §5.2 的「Cellium 官方 Sync 訂閱」

- **SelfHostedProvider** — 使用者自架後端
  - 給技術型使用者
  - 免費，但要自己處理運維

**架構好處**：三條 Provider 共用同一個 interface，UI 層完全不知道底下用哪個。使用者切換 Provider（從 LocalStorage 升級到 CloudStorage，或從 CloudStorage 改自架）對應的工程量幾乎為零。

**v0.5 校正**：v0.4 的 StorageProvider 計畫是「LocalStorage → CloudStorage 取代」的進化路徑。v0.5 改成「三條 Provider 並存、使用者選一條」的選項路徑。這個校正讓「寄生 drive」和「官方 Sync」可以共存，不互相排斥。

### 8.4 多平台路徑

| 階段 | 平台 | 重點 |
|---|---|---|
| 1 | Electron 桌面（Mac/Win/Linux） | 一套 codebase，三平台到位 |
| 2 | Web 版 | 把本地檔案層換成雲端 API，順勢上線付費服務 |
| 3 | 手機版（React Native） | 只做檢視 + 輕量編輯，不做完整畫布 |

---

## 九、開發節奏（v0.5 修訂）

### 9.1 v0.1 之後的剩餘範圍（v0.5 重估）

v0.1 已完成基礎，剩餘工作量重估（每週 6-8 小時 vibe coding 為基準）：

| 階段 | 內容 | 預估 |
|---|---|---|
| A | 拖檔案到畫布即建節點 + 多模態節點（icon + 雙擊開檔） | 2 週 |
| B | 圖片節點縮圖 | 1 週 |
| C | 範本系統（旅行整理、專案素材、論文研究三個範例） | 1-2 週 |
| D | Onboarding：第一次打開的範本選擇 + 空狀態提示 | 1 週 |
| E | 自由排佈優化（對齊輔助線、群組視覺、自動排佈選項） | 1-2 週 |
| F | 手動連線 + 邊顏色/樣式（進階表達力） | 2 週 |
| G | 節點原地展開為子畫布 | 2-3 週 |
| H | 自己用 + 找朋友試用 + 修痛點 | 持續 |

合計 **10-13 週**（每週 6-8 小時）= **2.5-3 個月**

v0.5 比 v0.4 多了範本系統（1-2 週）和 onboarding（1 週），這兩個是 v0.5 定位下不能跳過的。

若動力高拉到每週 15-20 小時，可壓縮到 **1.5-2 個月**。

### 9.2 AI Spotlight（MVP 之後的獨立階段）

不疊在 MVP 上。等 MVP 跑穩、找到真實使用者、知道他們找東西的真實 query pattern 之後再做。預估獨立 1-2 個月。

設計原則跟 v0.4 的 AI 翻譯層不同（見 §4.8）：不主動分類，只回應使用者查詢。

### 9.3 動力來源誠實標記（v0.5 校正）

**這是作者第一個以商業為主而非 dogfooding 驅動的開發項目。**

v0.5 校正 v0.4 的動力描述：除了「對方向的信念」之外，誠實補上**「賺錢也是驅動力」**。這不是壞事，但要對應到具體的回饋機制——商業上的回饋週期很長（可能兩三年才看到實質營收），中間靠什麼撐？

對比其他項目：

- **Cumulus**：每天用、每週發現新痛點就修。dogfooding 驅動，節奏穩定，動力內建。
- **戰旗 / Riposte**：休閒項目，興趣驅動，沒有時間壓力。
- **Cellium**：自己不一定每天用（Obsidian + Finder 對作者已經夠用）。動力來自「商業判斷 + 賺錢期望」，不是每天的痛感。

這個差異會影響開發體驗：

- Cumulus 的開發判準是「我自己用會不會更順」，這個訊號每天都在
- Cellium 的開發判準是「潛在使用者第一次打開會不會留下」，這個訊號要靠想像或測試獲得

**這意味著：**

1. 進度可能比 Cumulus 慢、波動大，這是正常的
2. 需要刻意做「使用者體驗」層的事（onboarding、範本、空狀態），這些事自己用不到但決定 90% 新使用者的去留
3. 完成 MVP 後**必須**找真實使用者測試（§7.4 判準二），不能只靠 dogfooding 驗證
4. 中間期動力管理：可以靠 Cumulus 進入慢車道（P3 商業化不急）釋放出的時間給 Cellium，避免動力消磨

**為什麼還是值得做：**

跨過這個階段本身就是價值——把「自己看的論文」升級到「給別人看的論文」這種寫作能力是複利型的，第一次跨過去之後，後面所有項目都會受益。Cellium 在商業上成不成功是一回事，這個轉變本身是值得投資的。

### 9.4 跟 Cumulus 的關係（v0.5 新增）

兩個產品同時跑要清楚分工：

- **Cumulus 進入慢車道**：P1/P2 自用功能繼續做、P3 商業化暫不啟動。沒有外部壓力，動力靠 dogfooding 維持。
- **Cellium 進入快車道**：v0.5 方向感清楚之後，這幾個月剛好是窗口（Obsidian 生態還沒完全把多模態收編、AI 還沒把整理需求消滅）

兩個產品的商業模式刻意不同：
- Cumulus 走 Notion 邏輯（買斷 + AI 訂閱）
- Cellium 走 Obsidian 邏輯（commercial license + sync 訂閱 + 一次性買斷 + AI 訂閱）

這意味著兩個產品的運營方式不同（前者是 SaaS 思維，後者是開源 + 服務思維），心態切換成本是真實的，要有意識管理。

## 十、行銷與定位敘事（v0.5 重寫）

### 10.1 核心論述：講使用者的痛點，不講產品的技術

**v0.5 校正**：v0.4 的論述（「graph-first 多模態知識編輯器」「節點泛型」「邊有語意」）是技術描述，使用者讀不懂也不在乎。Heptabase / Kosmik 失敗的核心原因之一就是把技術論述當行銷論述。

**v0.5 的對外論述都用使用者場景，不用技術詞彙**：

不寫：「Graph-first + 多模態的知識編輯器」
改寫：「**你的桌面亂嗎？你的雲端硬碟找不到東西嗎？Cellium 給你一個 2D 空間整理檔案。**」

不寫：「節點泛型 + 邊有語意」
改寫：「**把相關的東西放在一起，用空間記住它們的關係。**」

不寫：「本地優先 + 渲染層 + StorageProvider 抽象」
改寫：「**你的檔案還是你的檔案，Cellium 只是換個方式呈現它們。**」

### 10.2 不對標任何競品

v0.5 之前的論述（哪怕是 v0.4）還在跟 Obsidian、Heptabase、Notion 比。v0.5 完全不對標：

- 不寫「比 Obsidian 多模態」
- 不寫「比 Notion 視覺化」
- 不寫「比 Heptabase 商業模式好」

Notion 不對標誰，Obsidian 不對標誰，它們贏的時候是別人在跟它們比。Cellium 也用這個姿態：直接從使用者痛點出發，不在別人的座標系裡找位置。

### 10.3 兩個入口的分流論述

| 入口 | 場景 | 論述 |
|---|---|---|
| 個人桌面亂 | 拍的照片、下載的 PDF、隨手存的截圖散落各處 | 「Stacks 把你的桌面藏起來。Cellium 讓你用空間記住東西。」 |
| 雲端 drive 整治 | Google Drive 一堆亂七八糟的資料夾，找東西全靠搜尋 | 「你的 Drive 找不到東西？Cellium 給你視覺化整理層，原檔案放 Drive 不動。」 |

兩條入口的 onboarding 也對應分流：個人入口讓使用者拖本地檔案進來，drive 入口讓使用者指向 Drive 同步資料夾。

### 10.4 產品宣言（建議放在官網，v0.4 沿用）

> 我們相信你的資料是你的。
> 我們相信軟體應該為你服務，不是把你關起來。
> 你可以自己架伺服器、自己備份、隨時離開。
> 我們收的是幫你省麻煩的錢，不是逼你留下的錢。

v0.5 補：**「錢只能在對的地方收。」**（理念對齊 §5.1）

### 10.5 切入族群優先級

1. 自己（n=1，但動力來源是商業判斷而非每日痛感，要小心過度權重自己的需求）
2. 非技術朋友 3-5 人（v0.5 新增的硬性條件，§7.4 判準二）
3. 桌面亂、有檔案組織痛點的個人使用者
4. 重度使用 Google Drive / Dropbox 的個人或小團隊
5. 對 SaaS 失望、想要本地優先的知識工作者
6. Obsidian / Heptabase 重度使用者（覺得不夠用的人）—— 從第 1 移到 6，因為他們不是 Cellium 的主要目標

### 10.6 不要做的行銷動作（v0.5 新增）

從 Heptabase / Kosmik 學到的反面教材：

- ❌ 首頁就是 pricing（理念對齊 §5.1）
- ❌ 用 blog 寫產品開發歷程當主要 marketing 內容（使用者不在乎產品怎麼被做出來，只在乎能解決什麼問題）
- ❌ 要使用者先看 YouTube 教學才會用（產品本身就是教學，麥當勞問題的負面教材）
- ❌ 對標 X、Y、Z（會輸給 X、Y、Z）
- ❌ 強調功能列表（「無限畫布、卡片、AI 標籤、PDF 閱讀」這種列法對使用者沒意義）

## 十一、待決定（v0.5 更新）

從 v0.1 沿用：

- [x] ~~關係是否需要類型~~ → **已決：不預設類型，使用者用顏色/樣式自賦語義**
- [x] ~~節點細度~~ → **已決：MVP 文件層級，行間註解延後**
- [x] ~~UI layout~~ → **已決（v0.3）：三欄式，見下方說明**

### UI Layout（v0.3 確定）

三欄式 layout，參考 Obsidian 結構但右欄重新定義：

- **左欄（240px）**：資料夾樹 + 搜尋。MVP 先用資料夾結構，等畫布做出來、實際使用後再決定是否調整為純列表或其他形式。
- **中欄（flex）**：Editor / Graph 切換
  - **Editor 模式**：左寫右預覽的 Markdown 編輯器
  - **Graph 模式**：React Flow 2D 畫布，節點可拖拉、連線
- **右欄（280px）**：選中內容的摘要資訊，類似 Windows 檔案總管的「詳細資料」面板
  - Editor 模式：檔名、tags、建立日期、字數
  - Graph 模式：點到節點後顯示節點名稱、連結關係、md 內容前幾行預覽
  - 無選中：空白或工作區統計

v0.dev 已生成初版 layout 參考（light theme，Editor + Graph 兩個模式），可作為 Claude Code 的視覺起點。

v0.5 校正：UI layout 整體框架仍然成立，但**第一次打開的內容**要重做——不是空白畫布，是範本選擇畫面（旅行/專案/論文三選一），對應 §7.2 的 onboarding 設計。

v0.2 沿用：

- [ ] 節點展開的視覺呈現細節（卡片內嵌？切換視圖？動畫？）
- [ ] CRDT 同步引擎開源邊界
- [x] ~~AI 功能算服務還是核心？~~ → **已決（v0.5）：AI 是 Spotlight 式輔助，不是核心。雲端 AI 推論訂閱付費，自帶 API key 免費。詳見 §4.8**
- [ ] 自架伺服器的技術門檻設多高？要不要提供一鍵部署？
- [ ] 商業使用授權的具體定義（多少人算「商業」？參考 Obsidian 「2 人以上的公司」）
- [ ] Plugin API 設計（什麼開放什麼不開放）
- [x] ~~產品正式名稱~~ → **已決：Cellium，域名 cellium.app 已購入（2026-05-05）**

v0.4 沿用：

- [x] ~~AI 翻譯層的觸發時機~~ → **v0.5 廢棄：AI 改 Spotlight 模式，由使用者查詢觸發，不需要排程**
- [x] ~~AI 翻譯層的客觀 metadata schema~~ → **v0.5 廢棄：不做自動 metadata，改成被動回應查詢**
- [ ] AI 推論成本承擔方式（已決原則：自帶 API key 免費 + 官方雲端訂閱付費；具體訂閱定價未定）
- [ ] cellium.json schema 的版本化策略（為「未來可被其他工具讀寫」預留空間）
- [x] ~~MVP 後找潛在使用者測試的方式~~ → **已決（v0.5）：MVP 完成判準二強制找 3-5 個非技術朋友試用，詳見 §7.4**

v0.5 新增：

- [ ] 範本系統設計（旅行/專案/論文三個範例的具體內容、互動引導）
- [ ] drive 同步衝突的 UI 設計（畫布有衝突時怎麼讓使用者選版本）
- [ ] cellium.json 拆分粒度（一個 vault 一個檔？還是每個畫布一個檔？影響衝突機率）
- [ ] Provider 切換的使用者旅程（從 LocalStorage 升級到 CloudStorage 怎麼遷移？）
- [ ] App store 買斷的定價（$15 / $25 / $35 哪個甜蜜點？參考 Obsidian Catalyst $25）
- [ ] 多人即時協作的場景設計（個人整理檔案不需要即時協作，但團隊整理需要嗎？真實需求要驗證）

## 十二、定位驗證狀態（v0.2 新增）

**誠實狀態：產品差異性清楚，市場驗證未完成。**

### 已驗證

- ✅ 功能組合上跟現有競品有清楚差異
- ✅ 自己（作者）有強烈痛點，n=1 重度使用者已就位
- ✅ 商業模式、技術架構、產品哲學三層邏輯一致

### 未驗證

- ⚠️ 除作者以外的使用者是否有同樣痛點
- ⚠️ 使用者是否願意承擔「學新工具」的轉換成本
- ⚠️ 付費轉換率是否足以支撐長期開發

### MVP 後的驗證計畫

1. 自己用 3 個月，確認核心痛點被解決
2. 找 10-20 個目標使用者試用，看會不會自己回來繼續用
3. 觀察 Heptabase / Logseq / Anytype 的成長軌跡作為市場參照

---

## 十三、啟動條件

> 等到真實遊戲開發或產品開發遇到文件管理困境時再啟動。

現在先保留這份 PRD，痛點夠明顯時直接開始做，不用重新想一遍。

**v0.2 補充：** 文獻管理 / 論文寫作的痛點如果先到，也可以從這個場景啟動，因為產品已經拓寬為通用工具，啟動場景不限於 GDD。

---

## 附錄：產品哲學三層一致性

這是 v0.2 整理出來的關鍵發現——三層決策完全對齊：

| 層 | 決策 |
|---|---|
| **技術** | 本地優先、開放格式、（後期）開源 |
| **商業** | 自架免費、服務收費、商業授權 |
| **產品** | Graph-first、節點泛型、使用者掌控 |

很多產品技術選 A、商業選 B、產品選 C，三層互相打架。**這份 PRD 沒有這個問題，是難得的事**，也是這個產品成立的關鍵。

---

## 附錄 B：v0.4 戰略思考脈絡

這個附錄記錄 v0.4 更新背後的討論過程。決策本身已經寫進正文，這裡留的是「為什麼做這些決策」的脈絡，未來回頭看的時候可以參照。

### B.1 觸發點：Claudian 出現

Claudian 是 Obsidian 的 plugin，把 Claude Code 嵌進 vault 的 sidebar，讓 AI 自動建 wikilinks、整理筆記、查詢資料。2026 年初快速累積到 6,900+ stars。

這引發了對 Cellium 競爭力的重新評估：如果 Obsidian + Claudian 就能解決資料混亂，Cellium 還有存在價值嗎？

### B.2 第一輪推論：差距被拉近但結構性差異還在

**Claudian 解決的是「建立關聯」的摩擦**：原本要手動連的東西 AI 幫你做。這個確實減輕了 Obsidian 的一個主要痛點。

**Claudian 解決不了的是 Obsidian 底層資料模型的限制**：
- 關係還是無向、無語意的 wikilinks
- 節點類型還是只能是 .md 檔案
- 沒有可編輯的有方向、有類型的邊
- 沒有節點即子畫布的層級

**這指向一個更深的問題：Obsidian 為什麼不做多模態？**

答案是：違背他們的核心信念「筆記應該是純文字檔」。這個信念在 AI 時代意外成為優勢（plain text 對 AI agent 親和），但也成為他們動不了的限制。

### B.3 第二輪推論：盲點檢查

對「為什麼還沒有人做多模態 graph editor」這個問題做系統性盲點檢查：

| 盲點 | 結論 |
|---|---|
| 有人在做，只是不知道 | **真**：Kosmik、Heptabase 已經在跑 |
| 做出來但市場不買單 | **半真**：Heptabase 五六年沒爆發，但有付費小眾 |
| 工程成本個人吃不下 | **可避**：採 icon-only 不做內嵌預覽 |
| 護城河在生態不在功能 | **部分真**：瀏覽層架構降低資料遷移成本，但習慣遷移仍是門檻 |
| AI 五年內讓整件事不必要 | **真實風險**：但反方論證也站得住（建理解 vs 拿答案的分歧） |

最有殺傷力的是盲點一加盲點五的組合：「有人在做、跑得不夠快、五年後可能整個方向被 AI 改寫」。

### B.4 第三輪推論：Kosmik / Heptabase 輸在哪

它們不是內容輸，是商業模式 + 命名 + onboarding 輸。

- 首頁就是 pricing → 違反工具類產品「免費起家」的標準打法
- 進去無限畫布要使用者自己決定怎麼用 → 沒解決麥當勞問題
- 命名需要使用者先理解產品才能讀出來 → 口碑傳播硬傷

Cellium 在這三個維度都有條件避開（沒有 VC 壓力可走免費路線、命名構詞符合 Notion 等級、onboarding 可以打磨）。**避開這些不等於會贏，但起跑線沒有自帶障礙。**

### B.5 第四輪推論：Zotero 模式給的啟示

Zotero 真正贏的位置不是 reference manager，是 CSL —— 整個學術引用的「書同文車同軌」。Mendeley / RefWorks / Papers / Paperpile 都依賴 Zotero 維護的 CSL 樣式庫。

這給了 Cellium 兩個啟示：

**啟示一（戰術）：Zotero connector 的設計原則可直接用**

connector 抓 PubMed 的資料可能映射錯欄位，但抓出來永遠是 Zotero 統一 schema，使用者能改。準確性是次要的，**可編輯性 + 統一性**才是核心。

這個原則套到 Cellium 的 AI 翻譯層：AI 讀檔案產出 metadata 不一定準，只要輸出統一 schema、使用者能編輯、編輯後鎖定為事實來源——架構就站得住。

**啟示二（戰略）：規則制定者位置**

如果 cellium.json schema 能成為跨工具的多模態 metadata 標準，就有 Zotero 之於 CSL 的位置。但這是十年級工程，難處清楚（Adobe/Apple/Microsoft 反向激勵、採用慣性要時間、個人開發者沒有背書）。

### B.6 第五輪推論：動力來源誠實標記

對話過程中浮現的關鍵事實：作者對 Cellium 的動力主要來自「商業價值看得清楚」，不是「自己每天用」。Obsidian 對作者已經夠用。

這跟 Cumulus（每天用每週迭代）、戰旗/Riposte（休閒興趣）三個項目的動力來源都不同。Cellium 是作者第一個以商業為主驅動的項目。

這個誠實標記對開發節奏的影響：
- 進度會比 Cumulus 慢、波動大，正常
- 必須刻意做「使用者體驗」層的事，自己用不到但決定 90% 新使用者去留
- 不能只靠 dogfooding 驗證，MVP 後必須找真實使用者測試

### B.7 命名分析（順帶記錄）

Cellium = cell + ium。

- **cell**：國中英文，全球懂英文的人都認識，意象「細胞/單位/容器」
- **-ium**：拉丁字尾，元素（titanium、calcium）或場所（stadium、auditorium）
- **構詞邏輯**：類比 Notion = note + ion，是日常詞根 + 拉丁字尾造出來的「聽起來本來就存在」的字
- **發音**：cell-ium，兩個音節，跟 Notion 同等級

對比 Kosmik（cosmic 變體拼字、發音不確定）和 Heptabase（hepta- 字根對非英文母語者不直觀），Cellium 在命名品質上沒有自帶障礙。這不是「會贏」的證據，是「沒輸在起跑線」的確認。

### B.8 結論：對賭內容更新

v0.2 的對賭：產品差異性清楚，市場驗證未完成。

v0.4 的對賭（更新版）：
- 有人在做這個方向（Kosmik / Heptabase），但他們輸在商業模式 + 命名 + onboarding，Cellium 起跑線比他們高
- 多模態 + AI 翻譯層是真實的結構性差異，但這個差異有時間窗口（AI 多模態能力進步會壓縮這個窗口）
- 賭五年後使用者需要的是「建理解」而不是純粹「拿答案」。前者贏 Cellium 有護城河，後者贏 Cellium 死
- 動力來源是商業判斷而非每日痛感，需要靠對方向的信念而非每日反饋驅動

這個賭注沒有保證會贏，但盲點檢查完之後，作者選擇接受這個賭注。

---

## 附錄 C：v0.5 方向轉折脈絡

這個附錄記錄 v0.4 → v0.5 的方向轉折討論。決策本身已寫進正文，這裡留的是推導過程。

### C.1 觸發點：自我反思「我會不會看不到自己產品的真正優勢」

v0.4 完成後作者主動反思：Kosmik / Heptabase 看到的不是自己產品真正優勢，那我自己會不會也在同樣的循環裡？

這個自我反思指出三個可能的思路陷阱：

1. **痛點不是普遍的，是自己的**：自己技術型使用者覺得痛的東西，80 億人不一定痛
2. **看到的「結構性差異」對使用者不重要**：graph-first、多模態、節點泛型在工程上真實，但使用者愛產品不是為這個
3. **對賭基於「使用者會像我一樣思考」**：實際多數使用者不分析商業模式、不評估命名、不思考五年後的 AI

### C.2 校正關鍵：作者澄清自己也不是「層次三」使用者

對話過程中浮現的核心校正：

> 「層次三嗎？我覺得單是層次三我自己都不看好。我自己用也不太會層次三。我就是想把所有東西放一起，要看的時候不用翻來翻去。」

舉的具體例子：寫一個 txt 配一張圖，傳統上要嘛貼進 Word 插圖、要嘛新建資料夾把兩個東西放一起。所以多數人的桌面亂，到處都是新建文檔、新建資料夾。

這個校正把 Cellium 的定位從「graph-first 知識編輯器」（層次三：建立關係）下拉到「視覺化檔案整理層」（層次二：把相關東西放一起）。市場大十倍以上。

### C.3 Stacks 為什麼失敗 → Cellium 的核心價值

對話中浮現 Stacks 的問題：「Stacks 把東西藏起來，但你放久了你也不知道那個 stack 裡有什麼。」

這個觀察點出 Cellium 真正的差異：**Stacks 按系統規則分組，Cellium 按使用者意義分組**。

Cellium 的核心價值是「**空間記憶取代規則排序**」——實體世界你不會忘記鑰匙放在玄關桌上，因為空間位置就是記憶錨點。Stacks / 資料夾 / Finder 平面檢視都剝奪了這個能力，Cellium 還給使用者。

### C.4 從 visual canvas 到 better Finder 的關鍵跳躍

作者：「不，我不是一個 visual canvas，我要是一個 better Finder/Explorer。」

這個自我定位是質的不同：

- **Visual canvas**：給空白畫布讓使用者自己決定怎麼用（Heptabase / Kosmik 的麥當勞問題）
- **Better Finder**：打開就有使用者既有的檔案在裡面，直覺就是「整理一下」

差異一秒解決了麥當勞問題。同時也解釋了為什麼 v0.4 的「graph-first 多模態」聽起來繞——那些架構決策其實是 Finder 的延伸，不是 Notion 的延伸。

### C.5 對標關係：Notion 之於 Word

作者補上的重要校正：「不可能取代系統商的，原生永遠是底層邏輯。最好的情況是收編。不然比較可能的路線可能像是 notion 之於 word、notepad 之類的差異。」

這個類比指出：Cellium 不取代 Finder，是「第三種使用檔案的方式」。Notepad / Word 沒死、Notion 也活得很好，因為它們解決不同層次的問題。

### C.6 寄生在 drive 上的同步策略

作者：「我可以只整治本地，同步關係還是交給 drive 供應商，我只需要同步那個關鍵的 json 就好？這層很乾淨？」

這個架構決策乾淨到驚人：

- drive 客戶端負責檔案 + cellium.json 同步
- Cellium 完全不碰同步邏輯
- 跨裝置、版本控制、衝突解決全部外包給 drive

唯一要處理的是 cellium.json 本身的多人衝突，可以用拆檔策略 + CRDT-friendly 結構緩解。

### C.7 AI 從翻譯層降級為 Spotlight

作者：「AI 翻譯層 <- 一個詞：『spotlight』」

這個簡潔的比喻完成了 AI 角色的重新定位：

- v0.4：AI 翻譯層（自動產出客觀 metadata、使用者校對）
- v0.5：AI Spotlight 式輔助（不主動分類，使用者查詢時才回應）

降級的價值：AI 不再是核心功能，使用者熟悉的心智模型（Spotlight 大家都會用）取代了「AI 翻譯」這個需要解釋的概念。

### C.8 「錢只能在對的地方收」的根本原則

作者觀察 Notion 和 Obsidian：「個人版甚至同步完全不花錢，因為他根本不缺個體用戶的錢。」

這個觀察提煉成 §5.1 的根本原則：付費是「具體服務或便利」的對價，不是「使用權」的門票。

對應五個付費點：commercial license（B2B 合規）、app store 買斷（不想自己 build）、AI 雲端推論訂閱（不想自帶 key）、多人即時協作訂閱（drive 做不到的）、Cellium 官方 Sync 訂閱（不想用 drive）。每一個都有清楚的「使用者付的是什麼」。

### C.9 v0.5 對賭的更新

v0.4 的對賭：產品差異性清楚，市場驗證未完成。

v0.5 的對賭（更新版）：

- 從「層次三：建立關係」（小眾、不確定）改成「層次二：把東西放一起」（大眾、痛點普遍存在）
- 從「graph-first 多模態知識編輯器」（技術論述）改成「better Finder：用 2D 空間取代資料夾」（場景論述）
- 從「使用者願意學新工具」（高門檻）改成「使用者願意多用一個工具」（低門檻，類比 Notion 之於 Word）
- 從「自己做後端同步」（工程量大）改成「寄生在 drive 上」（架構乾淨）
- 從「AI 是核心翻譯層」（複雜）改成「AI 是 Spotlight 式輔助」（簡單）

這個賭注的具體賭點：

1. **桌面亂 / drive 亂的痛點足夠普遍**——非技術朋友會說「對，我也有這個問題」
2. **2D 空間記憶比資料夾好用**——使用者三個月後還能找到自己放的東西
3. **使用者願意「多用一個工具」**——不是換掉 Finder，是 Finder 之外再加一個 Cellium 用在特定場景

這個賭注比 v0.4 的「graph-first」實在得多——痛點具體、對手清楚（不是直接競品，是現有工具的不足）、產品價值一句話講得清楚（「用空間記住東西」）。但也不是穩贏，賭點都是真實的不確定性，必須靠 §7.4 判準二的真實使用者測試來驗證。

