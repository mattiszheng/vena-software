# 商業模式思考 — Notion 模式選擇

> 2026/05/19 對話記錄
> 情境：2A 雲端同步動工前，發現 STATUS P3 三層商業模式表跟 TECH_CloudSync 兩層模式漂移，回頭釐清商業模式的當前定位。

---

## 起點

STATUS 寫的「2A 雲端同步」段落有一句「同步是天然付費牆（免費單裝置、付費多裝置）」——這是 STATUS 最初的隨手寫法。但 TECH_CloudSync 寫的是「同步對所有人免費、付費層 = Stats + AI」。兩份完全相反。

回去翻 chat history 才發現：商業模式早在 2A 設計討論時就拍板過了，TECH_CloudSync 是定稿後的真實意圖，STATUS 那句是 stale 殘留沒同步。

漂移的原因：商業策略散在五處（TECH_CloudSync、STATUS P3 表、STATUS 2A 段落、PRD_AI_Assistant、PRD_Stats 暗示語），沒有單一 source of truth。

---

## 拍板的商業模式

**兩層，不是三層。**

| 層 | 包含 |
|---|---|
| 免費 | 完整 todo + calendar + 同步 + Routine + 遊戲化 + Stats 最近 90 天 |
| 訂閱 | Stats 完整歷史 + AI 助理（綁一起賣，不拆） |

**核心邏輯：**
- 沒有買斷層
- 同步免費，因為 2026 年用戶預期同步是基本功能，當付費牆會被嫌棄
- Stats 完整歷史 + AI 助理單獨賣吸引力都普通，綁一起才有「AI 用你的完整歷史給個人化建議」的整體感
- 為什麼是訂閱不是買斷：AI API 成本是持續的，訂閱才能 cover

**敘事一句話：**
> 免費版是完整的工具。訂閱是讓這個工具真正了解你。

---

## 為什麼選 Notion 模式

實質上選的是 Notion 的策略——**用免費版養品牌、用付費版養公司**。

Notion 的數字：
- 免費版能幹的事多到不可思議（無限頁面、無限 block、基本協作）
- 付費點在「團隊協作 + 進階管理 + AI」
- 免費用戶是「未來付費用戶的池子」+「自然推廣管道」

套用到 Cumulus 的關鍵點：**免費版不是試用，是完整體驗。**

幾個好處：

1. **口碑傳播成本低**。免費用戶推薦給朋友時不會尷尬（「免費好用」vs「不錯但要付錢」），轉介率高。
2. **付費動機不是 FOMO 是真實成長**。用戶用了三個月、累積夠多資料、想看完整歷史和個人化洞察——這時候才升級。轉換率可能低（5-10%）但留存率高。
3. **不用做「免費版要殘缺到什麼程度才能逼人付費」這種噁心設計**。

---

## 為什麼不選其他模式

之前考慮過、最後否決的方向：

| 模式 | 否決理由 |
|---|---|
| Things 3（買斷、全功能開包同步） | 紅海，這條路太擠 |
| Todoist（任務數量上限） | 噁心用戶體驗、現在用戶預期被養壞了 |
| Obsidian（同步獨立訂閱）| Obsidian 同步是真技術難題（端對端加密），Cumulus 不是 |
| 同步當付費牆 | 2026 年用戶預期同步是基本功能 |
| 單賣 Stats 完整歷史 | 用戶覺得「歷史本來就是我的」，吸引力普通 |
| 單賣 AI 助理 | ChatGPT/Claude 都有了，為什麼要多付一筆 |

關鍵體悟：Stats + AI 綁一起才有「整體感」——AI 的價值因為有完整歷史數據而變強，Stats 的價值因為 AI 的洞察而變大。兩個互相放大，不是單純疊加。

---

## 規模化前提

**Supabase 免費層撐得久：**
- 500 MB DB，純文字 task/tag/project 一個用戶終生資料約 1-5 MB → 可撐 100-500 個重度用戶
- 頻寬 5 GB/月，同步 payload 小 → 撐幾百個活躍用戶輕鬆
- 結論：到第一個 100 個用戶之前，完全免費

**真正開始燒錢的時點：**
- 500 重度用戶 → Supabase Pro $25/月
- 1000 用戶 → 可能要再升

到這個規模時，訂閱層該已經有真實收入。如果到 1000 用戶還沒人付訂閱——那不是「同步定價」的問題，是 Stats+AI 訂閱層本身沒價值，把同步當付費牆掩蓋不了真正問題。

**真正會無上限燒錢的是 AI API**——線性、每次 call 都要錢。所以 AI 必須是訂閱層、不能是免費。這個策略本身就對。

---

## 賭點

實質上是：

> 賭 Stats + AI 在「用了三個月以上」的重度用戶身上，能不能變成「拿掉就回不去」的價值。

這個賭法合理。但代表 P2-2C（遊戲化）、2D（Routine）、2E（Stats）這三塊**不能做殘缺**——它們是免費版完整體驗的關鍵，不是付費前的試用。做好了，用戶才會用三個月，才會升級。

這也回答了 Stats 的定位問題——Stats 不是「最後加上去的數據頁面」，是免費版價值的核心之一。

---

## Notion 模式的代價

要清楚這條路的成本，不能裝沒看到：

1. **前期收入慢**。免費用戶會先來，但前 1-2 年付費收入會很低，因為用戶還沒到「我需要更多」的階段。Notion 自己也熬了多年才賺錢。
2. **規模化才有效**。Notion 能活下來是因為它規模夠大、5% 付費 × 千萬用戶 = 50 萬付費。如果只有 100 個用戶，5% 付費 = 5 人，根本不夠養 Supabase Pro。
3. **必須有真正值得付費的東西**。Notion AI 確實提升了重度用戶生產力。Cumulus 的「Stats 完整歷史 + AI 助理」也要真的做到「用得越久越離不開」才行。

---

## 統計面要注意

同步免費 + 沒登入留資料在本地的設計，會有大量「下載了但不註冊」的用戶。這群人不會付錢、也不會燒錢（沒上雲）。

問題是統計上會看起來「DAU 高但付費低」，未來募資/評估時容易誤判。要在數據上把「註冊用戶」跟「匿名本地用戶」分清楚。

---

## 當前狀態（2026/05/19）

- 定價：訂閱層 $X/月，**TBD**，現在定價是猜。
- TECH_CloudSync §商業化邊界 = 事實上的權威表
- STATUS P3 表已對齊兩層模式（這次 plan 處理）
- 其他 PRD（PRD_AI_Assistant、PRD_Stats、PRD_Gamification）的商業描述次要 reference，未強制收斂

**未來真要再調整商業模式時，第一個動作是「先檢查所有商業相關 docs 是否一致」**——不要又散落。如果調整夠大，再做「商業策略收斂到單一文件」的 refactor。

---

*當前不開 BUSINESS_Model.md，理由：商業模式還在「拍板但未驗證」階段，提前固化反而妨礙迭代。等真實用戶反饋觸發第二次調整時再收斂。*
